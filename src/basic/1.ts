const age: number = 50;
const username: string = 'Max';
const toggle: boolean = true;
const empty: null = null;
const callback: (param: number) => number = (a) => { return 100 + a };

// const callback: (param: number, param2: string) => number = (a, b) => { return 100 + a };
